# DNA Chat V1 — owner release disposition @1

DNA_CHAT_V1_ENGINE=FROZEN

DNA_CHAT_V1_ACCEPTANCE=PASS_WITH_KNOWN_ISSUE

KNOWN_ISSUES=[ONEHOUR24-T03]

Frozen engine commit: `41f4d61b21a080f88d0363eec52dba64782f3040`.

Engine/application source identity (not a commit): `94155e19c111185440425be214c14ac7f1eb38107fbd662e3471a3450512186f`.

## Authority and immutable history

The product owner explicitly accepted V1 with the known non-safety semantic inconsistency in ONEHOUR24-T03. In one short shared example the object of the delayed action reverses in one sentence. This is a known product issue, not an assertion that the answer is correct. No T03 patch is authorized for this release.

The historical strict gate remains **FAIL**. Its raw judgments, Final70 answers, source-limited reviews and AI text-review record are unchanged. This owner disposition does not fabricate independent clinical validation or a new numerical judge score.

Final70: 70/70 executed, 65 PASS, four SOURCE_LIMITED_ACCEPTABLE, one known issue; runtime/500/503/empty zero, boundary 53/53, material accepted 186/188. Source-limited alternatives are not complete definitions. No new owner verdict is inferred for unrelated records.

## Application integration boundary

The UI release branch may repair build-only helper type compatibility. The eight legacy script adjustments handle the existing permission-denied result and populate already-required empty test-structure fields. They do not change product code, prompts, sources, gold, validators, judge code or model settings; the frozen source identity remains identical.

The resulting application integration commit is distinct from the frozen engine commit. It does not claim a newly run Final70 acceptance. No fresh benchmark, prompt tuning, semantic development or paid judge campaign is authorized.

Before promotion, require a successful production build and authenticated desktop/mobile critical UI smoke on this same engine: normal message, multi-turn history, new conversation, resume, safe boundary, source-limited answer, provider-error handling and session/history behavior. In-memory conversation continuity is distinct from page-reload persistence; the existing UI contract does not promise permanent transcript storage.

Do not infer LIVE from this disposition. Deployment, activation, current rollback identity and actual smoke results require separate evidence. Preserve the Report Engine and unrelated production work.

Reopen T03 only upon recurrence in real user use or evidence of a broader failure family. Cosmetic issues remain backlog.
