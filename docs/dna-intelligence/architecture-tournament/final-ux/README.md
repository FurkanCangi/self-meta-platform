# S13 Final Production Run

Tek komut:

```bash
npm run chat:s13:final-run:ssd
```

Komut hash bağlı checkpoint’lerden devam eder. Otomatik aşamalar tamamlandığında `awaiting_pro_evaluation` durumunda güvenli biçimde durur. `chatgpt-pro-evaluation-template.json` doldurulup `chatgpt-pro-evaluation.json` adıyla aynı SSD klasörüne konduğunda aynı komut 40 soruluk insan paketini hazırlar. Üç insan dosyası gelmeden kazanan veya public canary seçilmez.

Kanonik ham artefakt kökü:

`/Volumes/ResearchSSD/Outputs/SelfMetaAI/dna-intelligence/architecture-tournament/final-ux`
