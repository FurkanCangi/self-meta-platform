# DNA Architecture Tournament V2 — Faz 0

Frozen control: `9b54fd70411783e5f179f0cddc4564f33226447a`. Public runtime remains legacy; `DNA_ENGINE_VERSION=tournament` only enables shadow tournament work. `DNA_ENGINE_VERSION=legacy` is the single-setting rollback. Production deployment SHA is intentionally marked unverified because the Vercel scope returned 403. No secret values are stored.
